---
title: "Git指令汇总"
date: 2025-04-30
tags: ["Git"]
categories: ["工具备忘"]  
---

### 克隆远程仓库
1. 在电脑上选取（新建）一个用于存放该仓库内容的文件夹，打开
2. 在此处打开终端
3. 执行克隆命令： `git clone 仓库的网址`，这会在当前文件夹下创建一个与仓库同名的文件夹

### 提交并推送本地更改
1. 添加更改：`git add 文件名`( 或者添加所有更改：`git add .`)
2. 提交更改：`git commit -m "对本次更改的注明"`
3. 推送上线：`git push origin main`(`origin` 是远程仓库的默认别名，`main` 是分支名)

### 查看状态与历史

1. `git status`：查看工作区和暂存区的文件状态（哪些修改了、哪些已暂存）。
2. `git log`：查看提交历史。（`git log --oneline,`获得简洁版的日志)


### 拉取与更新(分支基础操作)
1. `git branch`：查看所有本地分支。
2. `git checkout -b 新分支名`：创建并切换到一个新分支。


